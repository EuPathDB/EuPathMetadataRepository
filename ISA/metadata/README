Directories are named the same as investigations.
The convention for these identifiers is Epoch time in milliseconds (The ISACreator GUI used this so we followed suit)

Investigation ids can be generated by this command:
$  date +%s%N | cut -b1-13

